// Legacy placeholder retained only because of tooling deletion limitation.
// Jest DOES NOT reference this file. All teardown handled in tests/jest.setup.js stopTestDb().
// Safe to remove manually (git rm jest.globalTeardown.js) in a future commit.
export default async function globalTeardown() { /* intentionally empty */ }
