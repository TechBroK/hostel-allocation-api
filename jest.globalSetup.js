// Legacy placeholder retained only because of tooling deletion limitation.
// Jest DOES NOT reference this file. All environment setup occurs in tests/jest.setup.js.
// Safe to remove manually (git rm jest.globalSetup.js) in a future commit.
export default async function globalSetup() { /* intentionally empty */ }
